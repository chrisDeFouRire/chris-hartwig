# AI Agent Guidelines

## Cloudflare Workers Types

⚠️ **IMPORTANT**: Do not use `@cloudflare/workers-types` as it's deprecated.

Instead, use the types generated by Wrangler:

```bash
npx wrangler types
```

This will generate the appropriate types automatically in `worker-configuration.d.ts`.
